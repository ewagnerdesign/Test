<%= text_field_tag "", "",
    former_control_options(control).merge({
        :class=>"inputDate"
    })
%>
<% content_for :form_scripts do  %>
  <%= date_picker(:control_id => control_id(control) ) %>
<% end %>
